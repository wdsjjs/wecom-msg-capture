# 开发约定

- 本仓库维护通用的 macOS 企业微信桌面采集、发送、历史恢复能力，以及使用虚构数据的独立消息工作台演示。
- `demo/` 是纯浏览器演示，不允许调用真实模型、设备或消息服务，也不读取桌面端配置和状态库。
- 示例只使用虚构名称、示例域名和测试标识，不引入组织品牌、内部地址或业务资料。
- 不加入本地配置、设备凭据、消息数据库、日志、图片或旧仓库历史。
- 默认不连接真实设备或服务，测试必须隔离状态并模拟网络和桌面操作。
- Git 分支和提交说明优先使用中文，不因普通修改另建分支。
- 只有用户明确要求时才推送 GitHub，不配置其他发布目标。
